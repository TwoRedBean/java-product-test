Question 4:

Can use PreAuthorize configuration on class level so that all methods within the class will inherit it also


Question 5:

SELECT p.Name, s.Sport, SUM(s.Score) AS "Total Score"
FROM players p
JOIN scores s ON p.Player_no = s.Player_no
ORDER BY s.Score DESC;